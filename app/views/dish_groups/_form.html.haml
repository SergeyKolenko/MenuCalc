= form_for @dish_group do |f|
  - if @dish_group.errors.any?
    #error_explanation
      %h2= "#{pluralize(@dish_group.errors.count, "error")} prohibited this dish_group from being saved:"
      %ul
        - @dish_group.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
