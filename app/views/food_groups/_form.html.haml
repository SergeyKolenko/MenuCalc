= form_for @food_group do |f|
  - if @food_group.errors.any?
    #error_explanation
      %h2= "#{pluralize(@food_group.errors.count, "error")} prohibited this food_group from being saved:"
      %ul
        - @food_group.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
